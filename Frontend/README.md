
  # Build my app idea

  This is a code bundle for Build my app idea. The original project is available at https://www.figma.com/design/hkJFlXGuIRbihHqT8X71k2/Build-my-app-idea.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  